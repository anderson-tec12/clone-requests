{
  "name": "clone-requests",
  "description": "Grave, consulte e repita requisições de API (fetch e XHR): URL, headers, payload, cURL e arquivo .http.",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare",
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:page": "npx --yes serve test-page -p 4173"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wxt": "^0.20.11"
  },
  "dependencies": {
    "axios": "^1.19.0"
  }
}
